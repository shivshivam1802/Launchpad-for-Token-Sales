{
  "name": "launchpad-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "contracts:build": "npm run --prefix contracts build",
    "contracts:test": "npm run --prefix contracts test",
    "backend:dev": "npm run --prefix backend start:dev",
    "backend:build": "npm run --prefix backend build",
    "frontend:dev": "npm run --prefix frontend dev",
    "frontend:build": "npm run --prefix frontend build"
  }
}
